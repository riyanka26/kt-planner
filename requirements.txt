# KT Planner Requirements
# This project uses Python standard library for CLI functionality
# Python 3.7 or higher required

# Web Interface Dependencies (optional - only needed for web app)
flask>=2.3.0
flask-cors>=4.0.0

# Core functionality uses only Python standard library:
# - json (for data storage)
# - datetime (for date/time handling)
# - argparse (for CLI)
# - typing (for type hints)
# - enum (for enumerations)

# To install web dependencies:
# pip install -r requirements.txt

# To run CLI only (no installation needed):
# python kt_cli.py --help